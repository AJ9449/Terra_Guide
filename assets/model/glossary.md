# Application Model — Glossary

**Application** — Model's container — groups every artifact that belongs to one system under test.

**Object Repository** — A stored set of element locators for one application or platform.

**Locator** — The specific selector TerrA uses to find a UI element on screen.

